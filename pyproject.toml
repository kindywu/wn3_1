[project]
name = "wn3-1"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "jsonschema>=4.26.0",
]

[tool.pytest.ini_options]
pythonpath = [".", "tests"]

[dependency-groups]
dev = ["pytest>=9.0.3"]
